/* import React from 'react';

class addButton extends React.Component {

  render() {
    const { add, product } = this.props;
    return (
      <div>
        <button type="button" onClick={() => add(product)}>
          Adicionar ao carrinho
        </button>
      </div>
    );
  }
}

export default addButton; */